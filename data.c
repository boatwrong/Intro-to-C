#include<stdio.h>
int main()
{
    int tacos = 5;
    // 5 is an example of data

    // 5 is an individual datum

    int half = 2;

    double double_taco = 5.0;
    double half_double = 2.0;

    tacos /= half;

    double_taco /= half_double;

    printf("%i  %f\n", tacos, double_taco);

    // primitive vs complex data

    // primitive is smallest data type, not divisible

// complex data is something like a struct
    // structs is a data type that we can define with different data types


